# 生活奇观资讯网站 · 完整搭建复盘记录

> 记录时间：2026-08-20
> 项目目录：`D:\MyProject\workbuddy\lifeFocus`
> 最终形态：Vue3 + FastAPI + MySQL，部署在腾讯云服务器（宝塔面板）+ 域名 + HTTPS

---

## 一、项目概述

| 项 | 内容 |
|---|---|
| 项目名称 | lifeFocus（生活奇观资讯网站） |
| 架构 | 前后端分离，后端统一托管前端构建产物 |
| 访问方式 | 单一域名 `https://www.shareworld.site`，由 Nginx 反代到后端 8000 端口 |
| 数据库 | MySQL 8.0（开发早期为 SQLite，后切换） |

---

## 二、技术栈

**前端**
- Vue 3（组合式 API）+ TypeScript
- Element Plus（UI 组件库）
- Vue Router 4 / Pinia（路由与状态）
- Axios（请求）/ Vite（构建）
- 风格：暖米白底 + 青玉色，高级编辑风

**后端**
- FastAPI（ASGI 框架）
- SQLAlchemy（ORM，支持 SQLite / MySQL 一键切换）
- Pydantic v2（数据校验，注意 `from_attributes=True`）
- PyJWT（登录鉴权）/ python-multipart（文件上传）
- uvicorn（ASGI 服务器）

**部署**
- 宝塔面板（Python 项目管理器 / Nginx / MySQL / Node.js 版本管理器）
- 腾讯云 DNSPod（域名解析）+ 腾讯云 SSL 证书
- 服务器：腾讯云（公网 IP）

---

## 三、本地开发阶段

1. **需求文档**：生成 `生活奇观资讯网站-需求文档.md`（12 章，含页面风格细节）。
2. **项目初始化**：在 `lifeFocus/` 下建 `backend/`（FastAPI）与 `frontend/`（Vue3）。
3. **早期 Bug 修复**
   - Pydantic v2 废弃 `orm_mode` → 改用 `from_attributes=True`（列表接口 500 修复）。
   - `init_db.py` 轮播依赖未提交数据 → 改为文章提交后再建轮播。
4. **前端三处优化**：去首页 tab 高亮 bug、去板块 tab 只留分类+排序居中、卡片用真实关联图片。
5. **数据库切换 SQLite → MySQL**：`database.py` 用 `DB_TYPE` 环境变量切换（`sqlite` / `mysql`），默认 `mysql`，密码经 `MYSQL_PASSWORD` 环境变量覆盖。
6. **Git 关联**：`git remote add origin https://github.com/15225030313/lifeFocus.git`（push 因凭据失败，需本地 PAT）。
7. **前端整版重做**：美观高级、逻辑合理。

---

## 四、服务器部署阶段（宝塔面板）

> 前提：已购腾讯云服务器、域名、SSL 证书、ICP 备案通过。

### 4.1 安装运行环境（软件商店）
- **Nginx**（Web 服务器 + 反代）
- **MySQL 8.0**（建库 `life_focus`，utf8mb4）
- **Python 项目管理器**（自带一个 Python 环境，如 `3.11.16`，路径 `/www/server/pyporject_evn/versions/3.11.16`）
- **Node.js 版本管理器**（服务器上 `npm run build` 用）

### 4.2 上传代码
- 本地打包 `lifeFocus.zip`（**不要**带 `node_modules`、`dist`、`app.db`、`__pycache__`）。
- 宝塔「文件」上传到 `/www/wwwroot/` → 解压得 `/www/wwwroot/lifeFocus/`。

### 4.3 构建前端（必须先做！）
宝塔「终端」：
```bash
cd /www/wwwroot/lifeFocus/frontend
npm install
npm run build
```
> 必须先在后端启动前 build 出 `dist`，否则后端找不到前端文件，首页返回 JSON。

### 4.4 部署后端（Python 项目）
- **添加项目**：
  - 项目名称：`backend`
  - Python 环境：选 `3.11.16`（面板自带的版本）
  - **启动方式：`命令行启动`**（没有 uvicorn 选项）
  - 启动命令：`uvicorn app.main:app --host 127.0.0.1 --port 8000`
  - 环境变量：`DB_TYPE=mysql`、`MYSQL_PASSWORD=宝塔MySQL的root密码`
- **安装依赖（关键）**：装进**宝塔自己的 Python 环境**，不是项目 venv：
  ```bash
  /www/server/pyporject_evn/versions/3.11.16/bin/pip install -r /www/wwwroot/lifeFocus/backend/requirements.txt --root-user-action=ignore
  ```
- **启动**：宝塔「启动」按钮（后台托管，开机自启）。

### 4.5 绑域名 + DNS 解析
- DNSPod：主机记录 `@` 和 `www`，类型 `A`，记录值 = 服务器公网 IP。
- 宝塔 Python 项目「设置 → 域名管理」添加 `www.shareworld.site` 和 `shareworld.site`。

### 4.6 配置 SSL
- 宝塔项目「设置 → SSL」，选「其他证书」，粘贴腾讯云下载的 Nginx 证书：
  - 证书（PEM/CRT）→ 证书框
  - 私钥（KEY）→ 密钥框
- 开启强制 HTTPS。

### 4.7 修复 Nginx 反代（解决 403）
默认配置把域名当静态文件服务器（`root /www/wwwroot/lifeFocus/backend`），无 `index.html` 且**没有 `proxy_pass`**，导致 403。
在网站「设置 → 配置文件」的 `server` 块内加入：
```nginx
location / {
    proxy_pass http://127.0.0.1:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```
保存后「软件商店 → Nginx → 重启」。

---

## 五、问题汇总表（踩坑全记录）

| # | 现象 | 根因 | 解决方案 |
|---|---|---|---|
| 1 | 点「设置」提示**环境准备失败** | 宝塔自动建 venv 时默认装 `uwsgi` 失败（网络/源问题），导致虚拟环境创建失败 | 终端手动建 venv + 清华源装依赖；或重装 Python 版本后重加项目 |
| 2 | 添加项目**没有 uvicorn 选项** | 宝塔启动方式只有「命令行启动 / uwsgi / gunicorn」 | 选 **「命令行启动」**，启动命令写完整 `uvicorn app.main:app --host 127.0.0.1 --port 8000` |
| 3 | 启动报 `(1045) Access denied for user 'root'@'localhost'` | 代码 `database.py` 默认密码 `Zcc117713190` 与宝塔 MySQL root 密码不符 | 在宝塔「环境变量」加 `MYSQL_PASSWORD=真实密码`（优先级最高），或改代码默认值 |
| 4 | 后端在终端跑起来了，但面板状态一直**未启动** | 终端手动跑的进程宝塔不知道，状态只反映面板托管进程 | 终端 `Ctrl+C` 停掉，改点宝塔「启动」按钮后台托管 |
| 5 | 点宝塔「启动」又变未启动，报 `ModuleNotFoundError: No module named 'fastapi'` | 宝塔用它**自己的 Python 环境**（`/www/server/pyporject_evn/...`），不是之前手动建的 `backend/venv`；那个环境没装依赖 | 用宝塔环境的 pip 装依赖：`/www/server/pyporject_evn/versions/3.11.16/bin/pip install -r requirements.txt` |
| 6 | pip 报 `WARNING: Running pip as the 'root' user...` | 仅警告，非错误（root 跑 pip 风险提示） | 加 `--root-user-action=ignore` 屏蔽；安装本身正常 |
| 7 | 浏览器访问域名报 **403 Forbidden** | Nginx 配置 `root` 指向 backend 目录（无 index.html）且**无 `proxy_pass`**，请求没转发给后端 | 在 Nginx 配置加 `location / { proxy_pass http://127.0.0.1:8000; ... }` 并重载 |

---

## 六、最终可运行配置速查

**后端启动命令**
```
uvicorn app.main:app --host 127.0.0.1 --port 8000
```

**环境变量（宝塔项目设置）**
```
DB_TYPE=mysql
MYSQL_PASSWORD=你的MySQL密码
```

**宝塔 Python 环境路径（装依赖用）**
```
/www/server/pyporject_evn/versions/3.11.16/bin/pip install -r /www/wwwroot/lifeFocus/backend/requirements.txt --root-user-action=ignore
```

**Nginx 反代核心段**
```nginx
location / {
    proxy_pass http://127.0.0.1:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

---

## 七、验证清单

- [ ] `https://www.shareworld.site` 前台首页正常，轮播/列表有图
- [ ] `https://www.shareworld.site/admin/login` 后台可登录（默认 `admin / admin123`）
- [ ] `https://www.shareworld.site/docs` Swagger 可访问
- [ ] 发布文章、上传图片功能正常
- [ ] 地址栏显示小锁（HTTPS 生效）
- [ ] 宝塔 Python 项目状态为「已启动」（绿色），关终端不影响

---

## 八、日常运维

| 操作 | 位置 |
|---|---|
| 看后端日志 | 宝塔 Python 项目 → 日志 |
| 重启后端 | 宝塔 Python 项目 → 启动/重启 |
| 改前端后生效 | 终端 `cd /www/wwwroot/lifeFocus/frontend && npm run build`，再重启后端 |
| 重启 Nginx | 软件商店 → Nginx → 重启 |
| 改 MySQL 密码 | 宝塔数据库 → root 密码（记得同步改环境变量 `MYSQL_PASSWORD`） |

---

## 九、安全加固（生产必做）

- MySQL 3306 **不对公网放行**（宝塔「安全」不要加该放行规则）。
- 后台默认密码 `admin123` 务必改掉。
- `backend/app/utils/security.py` 的 `SECRET_KEY` 建议改为环境变量读取。
- CORS 当前为 `*` + 允许凭据，同源下可改为仅放行你的域名。
- 宝塔面板端口/密码已改；服务器防火墙只留必要端口（8888/80/443）。
