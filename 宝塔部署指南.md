# 生活奇观资讯网站 · 宝塔面板部署指南（手动上传版）

> 适用：Vue3 + FastAPI + MySQL（已切换）前后端分离项目
> 操作方式：**全程在宝塔面板网页上点选完成**，代码**手动打包上传**（不从 git 获取）
> 目标：服务器装好环境 → 上传前后端代码 → 部署 → 用域名 + HTTPS 访问
> 前提：第一、二阶段已完成（已购腾讯云服务器、域名、已申请 SSL 证书、ICP 备案通过）

---

## 0. 整体结构（先看这张图，建立认知）

```
浏览器 ──https://你的域名──▶ 腾讯云 DNSPod（A 记录 → 服务器公网 IP）
                              │
                              ▼
                      宝塔面板（已装 Nginx）
                              │  自动生成反代：域名 → 127.0.0.1:8000
                              ▼
                  宝塔「Python 项目」托管 FastAPI
                              │  （uvicorn，开机自启，读环境变量）
                              ▼
                     MySQL（宝塔安装，仅内网 3306）

前端 dist 由后端统一托管（项目现状），宝塔只做“入口 + HTTPS + 反代”，
与你本地 http://localhost:8000 的运行模式完全一致，无需改任何业务代码。
```

**本指南的两条铁律**
1. 所有配置都在**宝塔网页面板**里点选完成，不用记 Linux 命令（仅「终端」里跑 `npm run build` 这类无法避免的构建命令）。
2. 代码**手动打包 → 宝塔文件管理器上传 → 解压**，不走 git。

---

## 1. 本地打包代码（在你自己的电脑上做）

> 只需打包「源码」，不要带 `node_modules`、`dist`、`app.db`、Python 缓存这些大文件/运行时数据，否则包很大且上传后还会报错。

**步骤：**
1. 打开 `D:\MyProject\workbuddy\lifeFocus` 文件夹。
2. 先删除以下几个（如果存在）：
   - `backend/app.db`（SQLite 旧库，用不到了）
   - `frontend/dist`（服务器上重新构建，不用传）
   - 任何 `__pycache__` 文件夹
3. 把整个 `lifeFocus` 文件夹压缩成 **`lifeFocus.zip`**：
   - **Windows 最简单**：右键 `lifeFocus` 文件夹 →「发送到」→「压缩(zipped)文件夹」。
   - 或用 7-Zip / WinRAR 右键压缩（选“仅存储”也可，zip 格式即可）。
4. 确认压缩包里**没有** `node_modules`、`dist`、`app.db` 这几样（其余都带上，尤其是 `backend/static/uploads` 里的封面图必须包含）。

> 也可以只传 `backend/` 和 `frontend/` 两个文件夹分别压缩，但打成一个 zip 最省事。

---

## 2. 安装宝塔面板（仅这一步需 SSH，可跳过）

> 如果你买服务器时「应用镜像」选了 **宝塔 Linux 面板**，面板已经装好，直接看第 3 步用镜像给的地址登录即可。

SSH 登录服务器后执行官方一键脚本（以 Ubuntu/Debian 为例）：
```bash
wget -O install.sh http://download.bt.cn/install/install-ubuntu_6.0.sh && sudo bash install.sh
```
装完终端会打印：
- 面板地址：`http://服务器IP:8888/随机字符`
- 用户名 / 密码

浏览器打开该地址登录。

### 2.1 面板安全（必做，登录后第一件事）
面板左侧 → **面板设置**：
- 修改「面板端口」（不要用默认 8888）
- 修改「管理员密码」
- 「安全入口」保留随机字符

面板左侧 → **安全**，放行端口：`8888`（面板）、`80`、`443`；**3306 不要放行**（数据库只走内网）。

---

## 3. 安装运行环境（软件商店，纯点选）

面板左侧 → **软件商店**，依次安装：
| 组件 | 说明 |
|---|---|
| **Nginx** | Web 服务器 + 反代（选最新稳定版） |
| **MySQL 8.0** | 数据库（装完到「数据库」设 root 密码） |
| **Python 项目 / Python 项目管理器** | 用来跑 FastAPI（可指定 Python 3.11） |
| **Node.js 版本管理器** | 用来在服务器上 `npm run build` 前端 |

### 3.1 在宝塔里建 MySQL 数据库
面板左侧 → **数据库** → **添加数据库**：
- 数据库名：`life_focus`
- 用户名：可自定（或就用 `root`）
- 密码：设置一个并**记住**（后面环境变量要用）
- 字符集：`utf8mb4`
- 提交

> 这一步相当于命令行里的 `CREATE DATABASE life_focus ...`，宝塔帮你建好了。

---

## 4. 上传代码（手动上传，不碰 git）

1. 面板左侧 → **文件** → 进入 `/www/wwwroot/` 目录。
2. 点左上角 **上传** → 选择你电脑上的 `lifeFocus.zip` → 开始上传。
3. 上传完成后，在文件列表里**右键 `lifeFocus.zip` → 解压**，解压到当前目录，得到 `/www/wwwroot/lifeFocus/`。
4. 确认目录结构正确：`/www/wwwroot/lifeFocus/backend`、`/www/wwwroot/lifeFocus/frontend` 都在。
5. （可选）删掉 `/www/wwwroot/lifeFocus.zip` 释放空间。

> 如果上传后 `backend/static/uploads` 里没有封面图（`.jpg`），说明本地打包漏了，把那些图单独上传补到该目录即可。

---

## 5. 构建前端（必须先做！）

面板左侧 → **终端**（宝塔内置，不是你电脑的终端），执行：
```bash
cd /www/wwwroot/lifeFocus/frontend
npm install
npm run build
```
> 必须先在后端启动前 build 出 `dist`，否则后端启动时找不到前端文件，首页只会返回 API 的 JSON。
> 之后每次改了前端代码，重新 `npm run build` 即可（后端会自动托管新 dist，建议重启一次后端确保生效）。

---

## 6. 部署后端（新版宝塔「Python 项目」管理器）

> 新版宝塔的「网站」页顶部有 PHP/Java/Node/Go/**Python项目** 标签页，切到 **Python项目**。
> 如果你已经添加了项目（列表里已有 `backend`，状态「未启动」），**不用重新添加**，直接从 6.2 开始。

### 6.1 添加项目（仅首次）
点 **添加项目**，按如下填写：

| 字段 | 填什么 |
|---|---|
| 项目名称 | `backend` |
| **Python 环境** | 先点右侧「**环境管理**」安装一个 3.9+ 版本（如 3.11），然后回来选它 |
| **启动方式** | 选 **「命令行启动」**（不是 uwsgi/gunicorn） |
| **项目路径** | `/www/wwwroot/lifeFocus/backend`（点文件夹图标选） |
| **启动命令** | `uvicorn app.main:app --host 127.0.0.1 --port 8000`（完整写出 uvicorn） |
| 环境变量 | 选「**指定变量**」，逐行填（见下方 6.4） |
| 安装依赖包 | 点文件夹图标选 `/www/wwwroot/lifeFocus/backend/requirements.txt` |

> ⚠️ 注意：新版宝塔的启动方式只有 **命令行启动 / uwsgi / gunicorn** 三种，没有独立的 uvicorn 选项。
> FastAPI 必须用 **「命令行启动」** + 启动命令里写完整 `uvicorn app.main:app ...` 的方式运行。
> 选 uwsgi 或 gunicorn 都不兼容 FastAPI 的 ASGI 模式，会报错。

### 6.2 安装依赖（必须先装，否则启动必报错）
点该项目操作列的 **模块**：
- 优先找「**从 requirements.txt 安装**」/「批量安装」入口，选择 `/www/wwwroot/lifeFocus/backend/requirements.txt`；
- 如果只能逐个安装，依次安装这些包：
  `fastapi`、`uvicorn`、`sqlalchemy`、`pymysql`、`pydantic`、`PyJWT`、`python-multipart`
- 等全部显示安装成功后再进行下一步。

### 6.3 检查启动配置（点「设置」）
点操作列的 **设置**，确认：
- Python 版本、启动方式 `uvicorn`、启动命令 `app.main:app --host 127.0.0.1 --port 8000` 无误；
- 已勾选 **开机启动**。

### 6.4 填写环境变量（连 MySQL 的关键，二选一）

**方式 A（推荐）**：在「设置」里找 **环境变量 / 项目配置**，逐行添加：
```
DB_TYPE=mysql
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=你第3.1步设的MySQL密码
MYSQL_DB=life_focus
```

**方式 B（找不到环境变量入口时）**：用面板「文件」编辑 `/www/wwwroot/lifeFocus/backend/app/database/database.py`，把这两行的默认值直接改掉：
```python
DB_TYPE = os.getenv("DB_TYPE", "mysql")                      # 默认改成 mysql
MYSQL_PASSWORD = os.getenv("MYSQL_PASSWORD", "你的密码")      # 引号里填真实密码
```

### 6.5 启动并验证
1. 回到项目列表，点状态处的 **未启动** 切换为启动（或在「设置」里点启动）。
2. 点 **日志**：看到 `Uvicorn running on http://127.0.0.1:8000` 即成功（后端会自动建表 + 写入默认管理员/分类/示例文章）。
3. 点操作列 **终端**，执行 `curl http://127.0.0.1:8000/api/articles`，返回 JSON 数据即后端 OK。

> 后端启动时会自动执行 `init_db()`：`create_all` 建表 + 写入默认管理员/分类/文章/轮播，无需你手动建表。

---

## 7. 确认环境变量已生效

环境变量在 6.4 已填好。这里只需确认一件事：**改完环境变量后，重启过一次项目**（列表里停止再启动），否则后端读不到新变量，会连不上 MySQL 或退回 SQLite。

> 判断方法：启动后看「日志」，若报 `Access denied` / `Can't connect to MySQL` → 密码或变量填错；若正常返回数据即生效。

---

## 8. 绑域名 + HTTPS（宝塔自动配 Nginx 反代）

1. 点该项目操作列的 **设置** → 找 **域名管理 / 绑定域名** → 添加：
   - `your-domain.com`
   - `www.your-domain.com`
2. 点项目 **SSL** 标签页：
   - 选 **腾讯云 SSL** → 把你在腾讯云申请的证书（Nginx 版 `fullchain.pem` / `privkey.pem`）上传
   - 或选 **Let's Encrypt** 一键免费签发
   - 开启 **强制 HTTPS**
3. 宝塔会自动生成 Nginx 反向代理：`域名 → 127.0.0.1:8000`，等效你本地 `localhost:8000`。

> 你之前申请好的 SSL 证书（第 2 阶段）在这里用上。证书文件在腾讯云 SSL 控制台下载「Nginx 格式」得到。

---

## 9. 域名解析（DNSPod）

腾讯云控制台 → **DNS 解析 DNSPod** → 你的域名：
- 主机记录 `@`，类型 `A`，记录值 = **服务器公网 IP**
- 主机记录 `www`，类型 `A`，记录值 = **服务器公网 IP**

等解析生效（几分钟~24h），浏览器访问 `https://your-domain.com`。

---

## 10. 验证清单

- [ ] `https://your-domain.com` 前台首页正常、轮播/列表有图
- [ ] `https://your-domain.com/admin/login` 后台可登录（默认 `admin / admin123`）
- [ ] `https://your-domain.com/docs` Swagger 可访问
- [ ] 发布文章、上传图片功能正常
- [ ] 地址栏显示小锁（HTTPS 生效）

### 常用运维（都在宝塔内）
- 看后端日志：Python 项目 → **日志**
- 重启后端：Python 项目 → **重启**
- 重新构建前端后：终端 `npm run build`，再重启后端
- 重启 Nginx：软件商店 → Nginx → **重启**

---

## 11. 排错表

| 现象 | 原因 | 解决 |
|---|---|---|
| 首页是 JSON 而非网页 | 前端没 build / dist 不存在 | 终端 `npm run build` 后重启后端 |
| 502 Bad Gateway | 后端没启动 / 端口不对 | Python 项目看日志，确认绑 127.0.0.1:8000 |
| 后台登录失败 / 接口 500 | 环境变量没填或密码错 | 确认 `DB_TYPE=mysql` + MySQL 密码，重启项目 |
| 图片 404 | uploads 目录没随代码上传 | 确认 `backend/static/uploads` 存在，补传封面图 |
| HTTPS 红标 | 证书路径错/未 reload | 在 SSL 标签页重新部署证书 |
| 数据库连接拒绝 | 3306 没开或 host 错 | MySQL 用 127.0.0.1，不要对公网放行 3306 |
| 访问域名“无法连接” | DNS 未生效 / 服务器未运行 | 等解析生效；看 Python 项目是否 active |
| 页面能开但 /api 报错 | 反代没配好 | 域名管理已加域名才会自动生成反代；检查 SSL/域名配置 |
| 点「设置」提示“环境准备失败” | 项目虚拟环境(venv)没建好，通常添加前没先在「Python环境管理」装好 Python 版本 | 先在「Python环境管理」装 3.9+ 版本 → 删除该 backend 项目 → 重新添加项目（选已装版本），宝塔会自动建好 venv |

---

## 12. 数据迁移（本地 → 服务器，可选）

如果你本地 MySQL 里已有文章/分类想原样搬：
1. 本地用 MySQL Workbench 或命令行导出：`mysqldump -u root -p life_focus > life_focus_dump.sql`
2. 宝塔 → **数据库** → 点 `life_focus` 的 **导入**，上传该 sql 文件导入。
3. 全新库可跳过，让 `init_db` 生成默认示例数据。

---

## 13. 安全与加固（生产必做）

- MySQL 3306 **不对公网放行**（宝塔「安全」里不要加这条放行规则）。
- 后台默认密码 `admin123` 务必在「系统管理 → 修改密码」改掉。
- `backend/app/utils/security.py` 的 `SECRET_KEY` 建议改为环境变量读取（JWT 密钥）。
- CORS 当前为 `*` + 允许凭据，同源下可改为仅放行你的域名。
- 宝塔面板端口/密码已改；服务器防火墙只留必要端口（8888/80/443）。

---

## 14. 时间线参考

1. 第 1 天：服务器装宝塔 + 环境；本地打包上传代码；构建前端；部署后端；绑域名 + HTTPS。
2. 备案已通过的前提下，DNSPod 解析后立即可用。
3. 后续改代码：本地改完 → 重新打包上传（覆盖对应文件）→ 终端 `npm run build` → 重启后端。
